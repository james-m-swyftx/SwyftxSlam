version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci --production
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
